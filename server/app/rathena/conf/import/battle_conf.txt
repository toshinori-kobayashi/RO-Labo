//--------------------------------------------------------------
// battle 設定の上書き（conf/battle_athena.conf の最後に import される）
//--------------------------------------------------------------
// 注意: rAthena の設定ファイルは行頭の "//" のみコメントとして扱われる。
//       値の右側に "//" を書くと値に含まれてしまうので書かないこと。
//
// 倍率はすべて percent 表記。100 = 1 倍。
// 変更手順: このファイル（リポジトリの app/rathena/conf/import/battle_conf.txt）を編集し、
//   ローカルで `terraform -chdir=terraform apply` を実行する。
//   このファイルは Docker イメージに焼き込まれるため、apply による再配布（イメージ再ビルド +
//   ro-server 再起動）で反映される。サーバ上で直接編集しても次回配布で上書きされる。
//--------------------------------------------------------------

//--------------------------------------------------------------
// EXP（conf/battle/exp.conf の既定はいずれも 100。2026-09-24 に 10 倍 → 100 倍へ変更）
//--------------------------------------------------------------
// Base EXP 100 倍
base_exp_rate: 10000
// Job EXP 100 倍
job_exp_rate: 10000
// MVP 撃破時ボーナス EXP 100 倍
mvp_exp_rate: 10000
// クエスト報酬 EXP 100 倍
quest_exp_rate: 10000

//--------------------------------------------------------------
// ドロップ（conf/battle/drops.conf の既定はいずれも 100）
// 値は元の確率に対する百分率（100 = 等倍）。実際の確率は item_drop_*_max（既定 10000 = 100%）で頭打ち。
//--------------------------------------------------------------
// 通常 Mob の一般アイテム 5 倍（据え置き）
item_rate_common: 500
// 通常 Mob の回復アイテム 5 倍（据え置き）
item_rate_heal: 500
// 通常 Mob の消耗品 5 倍（据え置き）
item_rate_use: 500
// 通常 Mob の装備 5 倍（据え置き）
item_rate_equip: 500

// Boss / MVP のカード以外 100 倍（2026-09-24 に等倍 → 100 倍へ変更）
item_rate_common_boss: 10000
item_rate_common_mvp: 10000
item_rate_heal_boss: 10000
item_rate_heal_mvp: 10000
item_rate_use_boss: 10000
item_rate_use_mvp: 10000
item_rate_equip_boss: 10000
item_rate_equip_mvp: 10000

// カード 1000 倍（2026-09-24 に通常 100 倍 / Boss・MVP 10 倍 → いずれも 1000 倍へ変更。
// 0.01% のカードは 10.00%。rAthena の上限は 1000000 = 10000 倍）
item_rate_card: 100000
item_rate_card_boss: 100000
item_rate_card_mvp: 100000

// MVP 報酬アイテム / 追加ドロップ（カード効果等） / 宝箱 100 倍（2026-09-24 に等倍 → 100 倍へ変更）
item_rate_mvp: 10000
item_rate_adddrop: 10000
item_rate_treasure: 10000
//   zeny_from_mobs: no（Mob からの Zeny ドロップ自体が無効。rAthena に zeny_rate キーは無い）
//   item_logarithmic_drops: no / drops_by_luk: 0（線形のまま）
//--------------------------------------------------------------

//--------------------------------------------------------------
// Mob 名の日本語化（conf/battle/monster.conf の既定は 0）
//--------------------------------------------------------------
// 0: スポーンファイルに書かれた表示名をそのまま使う
// 1: 常に mob_db の Name（英語名）を使う
// 2: 常に mob_db の JapaneseName を使う
// 2 にすると全 Mob の表示名が mob_db 由来になるため、
// npc/ 配下のスポーンファイルを 1 行も触らずに日本語化できる。
// 日本語名は overlay-utf8/db/import/mob_db.yml で Id + JapaneseName だけ書いて上書きする。
// JapaneseName が未定義の Mob は Name（英語名）にフォールバックする。
override_mob_names: 2
